'use strict';

/* jslint indent: 4 */

var filters = require('./filters.js');

module.exports = function() {
    return {
        root: process.env.ROOT || process.cwd(),
        deployDirs: ['src/', 'cfg/'],
        autoUndeploy: false,
        exit: false,
        detectServerVersion: true,
        lockContainer: undefined,
        lockContainerTimeout: 2 * 60 * 1000,
        workingSet: filters.invalidPathFilter(),
        includeFilter: filters.invalidPathFilter(),
        deploy: filters.invalidPathFilter(),
        treatUnmodifiedAsModified: false,
        undeploy: filters.invalidPathFilter(),
        parameters: {},
        treatWarningsAsErrors: false,
        simulateMake: false,
        stripCRFromCSV: false,
        singleDeleteCallsForDirectories: true,
        undeployFilename: 'undeploy.json',
        verbose: true,
        logFile: undefined,
        connectionTimeout: 10 * 1000
    };
};
